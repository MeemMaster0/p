export default async function handler(req,res){const u=new URL(req.url).searchParams.get("url");if(!u)return res.status(400).json({error:"URL required"});try{const t=u.startsWith("http")?u:"https://"+u,r=await fetch(t);const d=await r.arrayBuffer();res.setHeader("Content-Type",r.headers.get("content-type")||"text/plain");res.setHeader("Access-Control-Allow-Origin","*");res.send(Buffer.from(d))}catch(e){res.status(502).json({error:e.message})}}
